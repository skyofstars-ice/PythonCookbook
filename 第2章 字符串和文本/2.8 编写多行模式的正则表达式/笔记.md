&nbsp;&nbsp;问题：我们打算用正则表达式对一段文本块做匹配，但是希望在进行匹配时能够跨越多行。<br>
&nbsp;&nbsp;re.compile()函数可接受一个有用的标记————re.DOTALL。这使得正则表达式中的句点（.）可以匹配所有的字符。<br>
&nbsp;&nbsp;对于简单的情况，使用re.DOTALL标记就可以很好地完成工作。但是如果要处理极其复杂的模式，通常最好的方法是定义自己的正则表达式模式。